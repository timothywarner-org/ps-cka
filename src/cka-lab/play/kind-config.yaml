kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  # This section maps your local laptop port to the cluster
  extraPortMappings:
  - containerPort: 30080 # The NodePort we will set in File 2
    hostPort: 8080       # The port you type into your browser (localhost:8080)
- role: worker
- role: worker
